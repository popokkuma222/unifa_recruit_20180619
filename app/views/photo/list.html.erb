<h1>Photo List</h1>
<%= link_to "写真アップロード", photo_new_path %>
<% if @photos.present? %>
  <% @photos.each do |p| %>
    <%= p.title %>
    <%= image_tag '/photos/' + p.id.to_s + p.extention  %>
  <% end %>
<% end %>

